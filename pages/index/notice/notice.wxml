<!--pages/index/notice/notice.wxml-->
<view class="topline"></view>
<view class="container">
    <view class="title">{{detail.title}}</view>
    <view class="time">{{detail.updated_at}}</view>
    <view class="content">
        <rich-text nodes="{{detail.content}}"></rich-text>
    </view>
</view>
